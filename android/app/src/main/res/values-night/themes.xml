<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FoodFinder" parent="android:Theme.Material.NoActionBar">
        <!-- Edge-to-edge: draw behind transparent system bars (Activity calls enableEdgeToEdge()). -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
